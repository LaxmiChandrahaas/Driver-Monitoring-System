# Driver-Monitoring-System
A real-time driver drowsiness detection system using **Python**, **OpenCV**, and **MediaPipe** (Tasks API).   Monitors eye closure, blink patterns, yawning, and head posture via webcam.
